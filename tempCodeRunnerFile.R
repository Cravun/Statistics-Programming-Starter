data <- matrix(c(row1, row2, row3, row4), ncol=4, byrow=TRUE)
colnames(data) <- c("Col 1", "Col 2", "Col 3 ", "Col 4")
final <-  as.table(data)
final



# mean_row1 <- mean(row1)

# median_row2 <- median(row2)
# mean_row2 <- mean(row2)
# sort(row2)
# range_row2 <- range(row2)
 
# range_row2
# median_row2
# mean_row2